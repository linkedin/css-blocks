const foo = (
  asdf,
  xyz
) => {
  return true;
};

const foo = function(
  asdf,
  xyz
) {
  return true;
};

function foo(
  asdf,
  xyz
) {
}

class Foo {
  constructor(
    one,
    two
  ) {

  }
  foo(asdf: boolean, xyz: string) {
  }
  fooBar(
    asdf: boolean,
    xyz: "asdf"
  );
  fooBar(
    asdf: boolean,
    xyz: string
  ) {
  }
}

const f = new Foo(
 1,
 2);

(new Foo()).fooBar(
  true,
  "asdf");

foo(
  "asdf",
  "xyz");


@decorate(
"first",
"second)
const foo = 1;