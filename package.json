{
  "private": true,
  "scripts": {
    "commitmsg": "commitlint --edit $GIT_PARAMS",
    "ci": "lerna run test"
  },
  "devDependencies": {
    "@commitlint/cli": "^6.1.0",
    "@commitlint/config-conventional": "^6.1.0",
    "husky": "^0.14.3",
    "lerna": "^2.5.1"
  },
  "workspaces": [
    "packages/*"
  ]
}
